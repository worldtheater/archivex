Place `mermaid.min.js` in this directory for offline Mermaid rendering.

Expected path:
- `composeApp/src/androidMain/assets/mermaid/mermaid.min.js`

The app loads Mermaid only from local assets and blocks network access in the Mermaid WebView.
